# Copy to .env and fill in. .env is gitignored and must stay that way.
#
# Both values below are public by design: they ship inside the browser bundle,
# and RLS is what protects the data. Neither is a secret.
VITE_SUPABASE_URL=https://phfutvueuyxjkmzxrlzn.supabase.co
VITE_SUPABASE_ANON_KEY=

# NEVER put these in this file, in .env, or in any committed file. They bypass
# RLS entirely and belong only in the hosting provider's secret store:
#   SUPABASE_SERVICE_ROLE_KEY
#   the database password
